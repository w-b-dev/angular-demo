export enum CustomerType {
  contractor = 1,
  retail = 1.1,
  dealer = 0.8,
}

export enum SkylightModel {
  pvccm = 1,
  pvcsr = 1.15,
  ff = 1.2,
  ffsr = 1.38
}

export enum GlazingType {
  dome = 1,
  glass = 1.2,
}

export enum FrameFinishing {
  black = 1,
  mill = 0,
  brown = 0,
}

export enum DomeQuantity {
  single = 0.8,
  double = 1,
  triple = 1.45,
}

export enum TintedGlazing {
  yes = 1.15,
  no = 1,
}
